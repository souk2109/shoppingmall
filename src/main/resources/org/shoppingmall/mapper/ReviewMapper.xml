<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.shoppingmall.mapper.ReviewMapper">
	<insert id="insertReviewVO">
		insert into tbl_review(pno, id, orderNum, grade, review, firstItem, secondItem)
		values(#{pno}, #{id},#{orderNum}, #{grade}, #{review}, #{firstItem}, #{secondItem})
		<selectKey resultType="int" keyProperty="rno" order="AFTER">
			 SELECT LAST_INSERT_ID()
		</selectKey>
	</insert>
</mapper>
